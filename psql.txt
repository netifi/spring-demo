createdb marvel

psql marvel

CREATE TABLE records (
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    data jsonb NOT NULL
);

CREATE INDEX records_data_gin_idx ON records USING GIN(data);

cat records.json | psql marvel -c "COPY records (data) FROM STDIN;"

SELECT * FROM records WHERE (data -> 'images' ->> 'thumbnail') is not null;
