createdb marvel

psql marvel

CREATE TABLE records_txt (
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    data varchar(5000) NOT NULL
);

cat records.json | psql marvel -c "COPY records_txt (data) FROM STDIN;"